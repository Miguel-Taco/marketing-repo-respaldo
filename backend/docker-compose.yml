version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
      - DB_HOST=host.docker.internal
      - DB_PORT=3306
      - DB_NAME=marketing_crm
      - DB_USER=root
      - DB_PASSWORD=root
    restart: always
